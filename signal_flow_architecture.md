# 信号流程控制框图架构设计

## 1. 架构概述

本架构设计了一个通用的信号处理流程框架，支持以下核心功能：
- **Set Signal（设置信号）**：触发或设置信号状态
- **Check Signal（检查信号）**：检测信号当前状态
- **Wait（等待延迟）**：固定时间延迟
- **Timeout（超时控制）**：设置操作的最大等待时间
- **Duration（持续检测）**：在指定时间段内持续监控
- **Async（异步执行）**：并行执行多个操作

## 2. 核心组件定义

### 2.1 Signal Controller（信号控制器）
- 负责信号的设置、读取和状态管理
- 支持多种信号类型：布尔型、数值型、状态枚举
- 线程安全的信号存储

### 2.2 Time Manager（时间管理器）
- 管理 wait、timeout、duration 三种时间控制
- 提供精确的时间测量和超时检测
- 支持时间单位：ms、s、min

### 2.3 Signal Checker（信号检测器）
- 执行信号检查逻辑
- 支持条件表达式：等于、大于、小于、范围检查
- 返回检查结果：成功、失败、超时

### 2.4 Async Executor（异步执行器）
- 管理异步任务的创建和执行
- 支持任务并行、结果汇总
- 处理异步任务的超时和异常

## 3. 流程架构设计

### 3.1 基础流程结构

```
开始
  ↓
[设置信号] (Set Signal)
  ↓
[等待延迟] (Wait) - 可选
  ↓
[检查信号] (Check Signal) ← [超时控制] (Timeout)
  ↓                              ↓
判断结果                      超时处理
  ├─ 成功 → 继续
  ├─ 失败 → 重试/退出
  └─ 超时 → 超时处理
  ↓
结束
```

### 3.2 持续检测流程（Duration）

```
开始检测
  ↓
设置持续时间 (Duration)
  ↓
循环开始 ←──────┐
  ↓              │
检查信号         │
  ↓              │
记录结果         │
  ↓              │
判断：时间未到？──┘
  ↓ (时间到)
汇总结果
  ↓
结束
```

### 3.3 异步并行流程（Async）

```
主流程开始
  ↓
创建异步任务组
  ├─ 任务1：设置信号A → 检查信号A
  ├─ 任务2：设置信号B → 检查信号B
  └─ 任务3：等待 → 检查信号C
  ↓
并行执行（所有任务同时运行）
  ↓
等待所有任务完成（或超时）
  ↓
汇总结果
  ├─ 所有成功
  ├─ 部分失败
  └─ 超时
  ↓
主流程继续
```

## 4. 组合模式示例

### 4.1 带超时的信号检测

```
[设置信号: signal_name = True]
       ↓
[启动超时计时器: timeout = 5s]
       ↓
[循环检查信号] ←──────┐
       ↓               │
判断：信号满足条件？    │
  ├─ 是 → 成功退出    │
  └─ 否 ↓             │
判断：是否超时？       │
  ├─ 是 → 超时退出    │
  └─ 否 ───────────┘
```

### 4.2 持续时间检测（Duration with Check）

```
[设置监控信号]
       ↓
[启动持续检测: duration = 10s]
       ↓
[每隔interval检查一次] ←──┐
       ↓                   │
记录当前信号值            │
       ↓                   │
判断：持续时间未到？───────┘
       ↓
分析结果（所有值是否满足条件）
       ↓
返回结果
```

### 4.3 异步多信号协同

```
主任务
  ↓
启动异步任务组（带全局超时）
  ├─ 异步任务1
  │    ↓
  │  [设置信号A]
  │    ↓
  │  [等待2s]
  │    ↓
  │  [检查信号A]
  │
  ├─ 异步任务2
  │    ↓
  │  [等待1s]
  │    ↓
  │  [检查信号B，持续检测5s]
  │
  └─ 异步任务3
       ↓
     [设置信号C]
       ↓
     [等待0.5s]
       ↓
     [检查信号C，超时3s]
  ↓
等待所有任务完成（全局超时15s）
  ↓
汇总结果
  ↓
继续主流程
```

## 5. 状态转换图

```
[空闲] 
  ↓ 接收任务
[准备中] 
  ↓ 设置信号
[信号已设置]
  ↓ 开始检测
[检测中] ←──┐ 持续检测/重试
  ↓          │
判断         │
  ├─ 成功 → [完成]
  ├─ 失败 ───┘ (未超时则重试)
  └─ 超时 → [超时失败]
```

## 6. 错误处理机制

```
任何操作
  ↓
发生错误？
  ├─ 信号不存在 → 返回错误码
  ├─ 超时 → 返回超时状态
  ├─ 检测失败 → 记录失败原因
  └─ 异步任务异常 → 捕获异常并记录
  ↓
错误日志记录
  ↓
根据策略：重试 or 退出
```

## 7. 数据流向

```
输入参数
  ├─ 信号名称
  ├─ 信号值
  ├─ 时间参数（wait/timeout/duration）
  ├─ 检测条件
  └─ 异步配置
  ↓
信号控制器 → 时间管理器 → 信号检测器
  ↓
结果输出
  ├─ 状态码（成功/失败/超时）
  ├─ 信号值
  ├─ 执行时间
  └─ 错误信息（如有）
```

## 8. 典型应用场景

### 场景1：设备就绪检测
```
设置：ready_signal = False
等待：2s（设备初始化时间）
检查：ready_signal == True
超时：10s
结果：成功/超时
```

### 场景2：多传感器数据验证
```
异步执行：
  - 任务1：检测传感器A数据有效（超时5s）
  - 任务2：检测传感器B数据有效（超时5s）
  - 任务3：检测传感器C数据有效（超时5s）
等待所有任务完成（全局超时15s）
结果：所有传感器就绪 / 部分失败 / 超时
```

### 场景3：稳定性检测
```
设置：monitor_signal
持续检测：30s
检测间隔：0.1s
条件：signal_value > threshold
结果：统计在30s内有多少次满足条件
```

## 9. 架构优势

1. **模块化**：各组件职责清晰，易于扩展
2. **灵活性**：支持同步、异步、串行、并行等多种执行方式
3. **时间控制**：精确的超时和持续检测机制
4. **可组合性**：基础组件可以灵活组合成复杂流程
5. **错误处理**：完善的异常和超时处理机制
6. **可测试性**：每个组件可独立测试

## 10. 实现建议

- 使用状态机模式管理流程状态
- 使用观察者模式实现信号通知
- 使用线程池/协程池管理异步任务
- 使用配置文件定义复杂流程
- 提供可视化监控界面显示实时状态
